export interface UserPhoneNumberData {
  username: string;
  phoneNumber: string;
}

export interface PhotoData {
  title?: string;
  link: string;
}

export interface DialogMessage {
  title?: string;
  message: string;
}
